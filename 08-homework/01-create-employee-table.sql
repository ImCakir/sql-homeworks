-- Soru 1: test veritabanınızda employee isimli sütun bilgileri id(INTEGER),
 name VARCHAR(50), birthday DATE, email VARCHAR(100) olan bir tablo oluşturalım.

-- 1. employee tablosunu oluşturma
CREATE TABLE employee (
    id INTEGER,
    name VARCHAR(50),
    birthday DATE,
    email VARCHAR(100)
);
